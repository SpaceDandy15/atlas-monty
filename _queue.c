#include "monty.h"

/**
 * _queue - funciton that sets the format of teh data to a queue (FIFO)
 * @stack: double pointe ot hte head of stack
 * @line_number: script line number
 *
 * Return: no return
*/
void _queue(stack_t **stack, unsigned int line_number)
{
	(void)(stack);
	(void)(line_number);
	var.queue = QUEUE;
}
